import styled from "styled-components";

// export const Wrapper = styled.div<{ spacing: number }>`
//   display: flex;
//   flex-flow: column nowrap;
//   gap: ${(p) => `${p.spacing * 0.25}rem`};
// `;

export const Wrapper = styled.div`
  display: flex;
  flex-flow: column nowrap;
`;
